# DBG Demo: Monorepo-Multiworkspace

This repository demonstrates the multiworkspace pattern: 20 landing zones managed in a single monorepo, each with its own HCP Terraform workspace and state file, triggered via path filters.
